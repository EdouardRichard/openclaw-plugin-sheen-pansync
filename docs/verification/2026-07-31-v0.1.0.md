# v0.1.0 验证记录（2026-07-31）

## 门禁结论

```text
Automated gate: PASS
OpenClaw integration smoke gate: FAIL
Real Aliyun account gate: NOT RUN
Package inspection gate: PASS
Release decision: BLOCKED
```

自动化和包内容均通过，但 OpenClaw 集成 smoke 的全部必需条件没有在本环境中完成。没有提供专用阿里云盘应用、账号或初始 Refresh Token；真实账号矩阵不得推断为成功，当前版本不得按本记录发布。

## 验证基线与制品

- Reviewed commit: `85ccc56a31c507a50af0f3f88928c7b5abbd5496`
- Node.js: `v22.23.1`（通过 Volta 固定）
- OpenClaw: `2026.7.1-2 (0790d9f)`
- Artifact: `openclaw-pan-sync-helper-0.1.0.tgz`
- SHA-256: `0faa5a6f74f7b7e579e6feac248205ba5fb467c2d35e9f61d0d421289bd19e70`
- Size: `61151` bytes
- npm pack file count: `77`

从已审查提交的干净 tracked 内容只生成了一个实际 tarball；工作区原有未跟踪 `dist/` 与 `node_modules/` 未用于制品、未被打包，也未被改写。JSON 包检查确认所有必需运行时入口、UI、Skill、manifest 与 README 均存在；`src/`、`tests/`、`node_modules/`、`.superpowers/`、插件私有状态、密钥、凭证和环境文件均未进入包。

## 无凭证发现与注册证据

实际 tarball 通过官方 `openclaw plugins install` 安装到新的隔离状态，安装返回成功。随后使用官方 OpenClaw 命令验证：

- `plugins inspect pan-sync-helper --runtime --json`：插件 `loaded`、enabled、activated；`pan_sync_upload` 已注册，`pan-sync` CLI descriptor 已注册，HTTP route count 为 `1`，diagnostics 为 `0`。
- `plugins doctor`：退出码 `0`，显示无插件问题。
- `skills info pan-sync-upload --json`：Skill eligible、未禁用、model/user/command visible，且 bins、environment、config、OS 均无缺失要求。

补充的 installed-artifact runtime probe 仅用于读取实际安装入口的 Tool 与 Control UI 注册结果，不替代任何官方 CLI 证据：

- Tool 名为 `pan_sync_upload`；在隔离工作区且 Vault 为空时返回的安全 details 恰为 `{ "code": "CREDENTIALS_REQUIRED" }`。
- 已安装 Skill 同时包含 `CREDENTIALS_REQUIRED` 与 `openclaw pan-sync configure`，提供不含敏感值的设置指引。
- Control UI 只注册一个 `tab` descriptor：id `pan-sync-helper`，path `/plugins/pan-sync-helper/status`，唯一 required scope 为 `operator.write`。

## 官方配置流程与浏览器证据

实际安装的 `openclaw pan-sync configure` 成功到达 readiness；没有 plugin registration failure 或 unknown-command 分类。原始 CLI 输出只保留在待清理的隔离临时根中，未复制到本文。

浏览器验收使用 Codex in-app Browser 的 browser-client 运行时，按实际生成的 loopback URL 选择浏览器；没有使用 standalone Playwright、Selenium、Chrome automation、Computer Use 或内部 configure handler harness。

| 条件 | 状态 | 已净化证据 |
| --- | --- | --- |
| 仅监听 `127.0.0.1` | PASS | OS socket 检查只有一个 `127.0.0.1` listener；没有 `0.0.0.0` 或 IPv6 wildcard listener。 |
| 页面首次加载后移除 fragment | PASS | 实际页面 title 正确，地址仍为 loopback origin，hash 为空；页面状态为 `READY`。 |
| 凭证字段完整可见 | PASS（无效候选） | 三个 input 都是 `type="text"` 且 `autocomplete="off"`；没有记录或输出其值。 |
| 无效候选不替换旧记录 | PARTIAL / NOT RUN | 三个明确非秘密的无效候选返回 `CREDENTIALS_INVALID`；提交前后均无 `credentials.enc` 或 `master.key`，证明未创建记录。但没有有效旧记录，因此“替换已有记录”分支未运行。 |
| 有效记录在同一 tab reload 后完整可见 | NOT RUN | 没有专用有效凭证，不得声称该条件通过。补充观察：无效候选后同 tab reload 保持 hash 为空，但页面清除一次性授权并显示 `REQUEST_FAILED`，三个字段为空。 |
| save/clear 完成后关闭 server | PASS（clear 分支） | 空 Vault 的 clear 需要显式二次确认，返回 `CREDENTIALS_CLEARED`；随后实际 listener 和官方 CLI process tree 均在结果展示窗口内退出。有效 save 分支未运行。 |
| 十分钟超时关闭 | NOT RUN | clear 分支已结束 server，未另等十分钟超时。 |
| 远程 SSH tunnel | NOT RUN | 本次没有可用的远程 Linux OpenClaw 主机。 |
| `0700/0600/0600` 文件模式 | NOT RUN / UNAVAILABLE | 当前为 Windows host，不能提供受支持的 POSIX mode 验收。 |

因为表内必需条件并非全部 `PASS`，`OpenClaw integration smoke gate` 必须为 `FAIL`。

## 真实阿里云盘账号矩阵

没有提供专用阿里云盘测试应用或账号。以下每一项均为 `NOT RUN`：

1. 通过第三方 custom-client 流程取得初始 Refresh Token — `NOT RUN`。
2. 保存并验证匹配的 Client ID、Client Secret、Refresh Token — `NOT RUN`。
3. 上传小文件到 `/openClawShare` — `NOT RUN`。
4. 同名再次上传并确认自动重命名 — `NOT RUN`。
5. 上传大于 40 MiB 的文件并确认 multipart 完成 — `NOT RUN`。
6. 两文件中故意让一个失败并确认 partial result — `NOT RUN`。
7. 强制 access-token 过期，直接刷新并持久化旋转后的 Refresh Token — `NOT RUN`。
8. 重启 OpenClaw 后再次上传 — `NOT RUN`。
9. 验证中英文触发短语 — `NOT RUN`。
10. 检查净化日志与对话输出是否泄漏凭证或路径 — `NOT RUN`。

## 自动化与仓库检查

```powershell
volta run --node 22.23.1 npm run verify
git diff --check
```

新鲜完整 verify 返回 `0`：typecheck 通过；unit 为 11 files / 173 tests passed、1 个 Windows 平台不适用用例 skipped；integration 为 7 files / 90 tests passed；build 与 package dry run 通过，dry run 为 77 files。`git diff --check` 无 whitespace error。

## 自检与关注项

- 没有把动态端口、一次性 URL fragment、临时主机路径、外部原始路径、凭证、token 或原始日志写入本文。
- 没有把 handler harness 当作官方 CLI；官方安装、runtime inspect 与 `pan-sync configure` 均来自实际安装的 tarball。
- 没有声称有效记录 reload、有效 save、远程 SSH tunnel、POSIX mode 或真实账号上传通过。
- 同 tab reload 会清除页面的一次性授权并落入 `REQUEST_FAILED`；没有有效凭证时不能判定有效记录是否仍可投影，因此该项保持 `NOT RUN`，并继续阻止发布。
- 本记录只更改 README 与本验证文件；生产源码、测试、package、计划和 ledger 均未修改。
